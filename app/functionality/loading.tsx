import { FC } from "react";

// A loading file can create instant loading states built on React Suspense.
const FunctionalityLoading: FC = () => {
  return <section>Functionality Loading...</section>;
};

export default FunctionalityLoading;
